<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xue.mapper.ProductMapper">
    <insert id="insert" parameterType="Product" keyColumn="id" useGeneratedKeys="true" keyProperty="id">
        insert into product(name, subTitle, originalPrice, promotePrice, stock, cid, createDate)
        values (#{name}, #{subTitle}, #{originalPrice}, #{promotePrice}, #{stock}, #{cid}, #{createDate})
    </insert>

    <select id="getProducts" resultType="Product">
        select * from product
    </select>

    <select id="getProductByID" parameterType="int" resultType="Product">
        select * from product where id = #{id}
    </select>

    <delete id="delete" parameterType="int">
        delete from product where id = #{id}
    </delete>

    <update id="update" parameterType="Product">
        update product
        set name = #{name}, subTitle = #{subTitle}, originalPrice = #{originalPrice}, promotePrice = #{promotePrice}, stock = #{stock}, cid = #{cid}, createDate = #{createDate}
        where id = #{id};
    </update>

    <select id="getProductsByCid" parameterType="int" resultType="Product">
        select * from product where cid = #{cid}
    </select>
</mapper>