<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xue.mapper.OrderItemMapper">
    <insert id="add" parameterType="OrderItem" keyProperty="id" useGeneratedKeys="true">
        insert into orderitem(pid, oid, uid, number)
        VALUES (#{pid}, #{oid}, #{uid}, #{number})
    </insert>

    <delete id="delete" parameterType="int">
        delete from orderitem where id = #{id}
    </delete>

    <update id="update" parameterType="OrderItem">
        update orderitem
        set pid = #{pid},
            oid = #{oid},
            uid = #{uid},
            number = #{number}
        where id = #{id};
    </update>

    <select id="list" resultType="OrderItem">
        select * from orderitem
    </select>

    <select id="getOrderItemById" parameterType="int" resultType="OrderItem">
        select * from orderitem where id = #{id}
    </select>

    <select id="getOrderItemByOid" parameterType="int" resultType="OrderItem">
        select *
        from orderitem
        where oid = #{oid};
    </select>
</mapper>